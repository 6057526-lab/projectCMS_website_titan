# Краткий список переменных окружения для Railway

## ✅ Уже настроено (обязательные):

### 1. DATABASE_URL ✅
- **Статус:** Уже настроено Railway автоматически
- **Где:** Railway Dashboard → PostgreSQL → Variables
- **Действие:** Ничего не делать

### 2. JWT_SECRET ✅  
- **Статус:** Уже добавлено вами
- **Где:** Railway Dashboard → Next.js сервис → Settings → Variables
- **Действие:** Ничего не делать

---

## ⏳ Cloudinary (для будущей интеграции):

Cloudinary используется для загрузки и оптимизации изображений. **Сейчас это не обязательно**, но когда будете добавлять загрузку картинок в админке, понадобятся эти переменные:

### 3. CLOUDINARY_CLOUD_NAME (опционально, для будущего)
- **Описание:** Cloud name вашего аккаунта Cloudinary
- **Где взять:** После регистрации на [cloudinary.com](https://cloudinary.com)
- **Расположение в Railway:**
  - Railway Dashboard → Ваш проект → Settings → Variables → Add Variable
  - Имя: `CLOUDINARY_CLOUD_NAME`
  - Значение: `<ваш-cloud-name>`

**Пример:**
```
CLOUDINARY_CLOUD_NAME=my-company-name
```

### 4. CLOUDINARY_API_KEY (опционально, для будущего)
- **Описание:** API Key для загрузки изображений
- **Где взять:** Cloudinary Dashboard → Settings → API Keys
- **Расположение в Railway:**
  - Railway Dashboard → Ваш проект → Settings → Variables → Add Variable
  - Имя: `CLOUDINARY_API_KEY`
  - Значение: `<ваш-api-key>`

### 5. CLOUDINARY_API_SECRET (опционально, для будущего)
- **Описание:** API Secret для загрузки изображений
- **Где взять:** Cloudinary Dashboard → Settings → API Keys
- **Расположение в Railway:**
  - Railway Dashboard → Ваш проект → Settings → Variables → Add Variable
  - Имя: `CLOUDINARY_API_SECRET`
  - Значение: `<ваш-api-secret>`

### 6. NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME (опционально, для будущего)
- **Описание:** Публичный Cloud Name для использования на клиенте
- **Примечание:** Переменные с префиксом `NEXT_PUBLIC_` доступны на клиенте
- **Расположение в Railway:**
  - Railway Dashboard → Ваш проект → Settings → Variables → Add Variable
  - Имя: `NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME`
  - Значение: `<ваш-cloud-name>` (тот же, что и CLOUDINARY_CLOUD_NAME)

---

## 📋 Текущий статус переменных

### ✅ Обязательные (уже настроено):
- [x] `DATABASE_URL` - автоматически от Railway PostgreSQL
- [x] `JWT_SECRET` - для админки (уже добавлено вами)

### ⏳ Опциональные для будущего:
- [ ] `CLOUDINARY_CLOUD_NAME` - только когда добавите загрузку картинок
- [ ] `CLOUDINARY_API_KEY` - только когда добавите загрузку картинок
- [ ] `CLOUDINARY_API_SECRET` - только когда добавите загрузку картинок
- [ ] `NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME` - только когда добавите загрузку картинок

---

## ✅ Итого: Сейчас нужно только 2 переменные (обе уже настроены!)

**Для админки:**
- ✅ `DATABASE_URL` - уже есть
- ✅ `JWT_SECRET` - уже есть

**Для Cloudinary:**
- ❌ Пока не нужно (планируется в будущем)
- Когда будете добавлять загрузку картинок - добавите переменные Cloudinary

---

## 🔐 Безопасность

- ⚠️ **Никогда не коммитьте** `.env` файлы в Git
- ⚠️ **Секретные ключи** (JWT_SECRET, CLOUDINARY_API_SECRET) - только на сервере
- ✅ **Публичные переменные** (NEXT_PUBLIC_*) - доступны на клиенте, безопасны для публикации

---

## 📝 Когда добавлять Cloudinary переменные?

Добавьте эти переменные только когда:
1. Зарегистрируетесь на [cloudinary.com](https://cloudinary.com)
2. Создадите бесплатный аккаунт (есть бесплатный tier)
3. Решите добавить загрузку картинок в админке
4. Установите пакет `cloudinary` в проект

**Пока это не обязательно** - админка работает только с текстом.

