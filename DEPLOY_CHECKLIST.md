# Чек-лист перед деплоем на Railway

## ✅ Подготовка проекта

### 1. Git репозиторий
- [ ] Проект находится в Git репозитории (GitHub/GitLab/Bitbucket)
- [ ] Все изменения закоммичены
- [ ] `.env` файл в `.gitignore` (проверено автоматически)
- [ ] Проект запушен в удаленный репозиторий

**Команды:**
```bash
git status                    # Проверить статус
git add .                     # Добавить файлы
git commit -m "Ready for deploy"
git push                      # Запушить в удаленный репозиторий
```

---

## ✅ Создание проекта на Railway

- [ ] Зарегистрирован аккаунт на [railway.app](https://railway.app)
- [ ] Создан новый проект
- [ ] Подключен Git репозиторий (GitHub/GitLab)
- [ ] Railway автоматически определил Next.js проект

---

## ✅ PostgreSQL база данных

- [ ] Создан PostgreSQL сервис в Railway проекте
- [ ] База данных запущена и работает
- [ ] Записан/скопирован `DATABASE_URL` из Variables базы данных

**Где найти:**
- Railway Dashboard → Ваш PostgreSQL сервис → Variables → `DATABASE_URL`

---

## ✅ Переменные окружения

### Обязательные переменные:

- [ ] **DATABASE_URL** 
  - Автоматически создан Railway при добавлении PostgreSQL
  - Доступен для всех сервисов в проекте
  
- [ ] **JWT_SECRET**
  - Сгенерирован случайный ключ (минимум 32 символа)
  - Добавлен в основной сервис (не в базу данных)
  
**Где добавить JWT_SECRET:**
- Railway Dashboard → Ваш основной сервис (Next.js) → Settings → Variables → Add Variable

**Генерация JWT_SECRET:**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

- [ ] **NODE_ENV** (опционально)
  - Установлено значение `production`

---

## ✅ Первый деплой

- [ ] Railway автоматически начал сборку после подключения репозитория
- [ ] Сборка успешно завершилась (зеленый статус)
- [ ] Приложение запустилось без ошибок

**Где проверить:**
- Railway Dashboard → Ваш сервис → Deployments → Последний деплой

---

## ✅ Миграции базы данных

- [ ] Запущены миграции Prisma
- [ ] Миграции успешно применены

**Команда:**
```bash
railway run npx prisma migrate deploy
```

**Или через Railway Dashboard:**
- Используйте Railway CLI или Deploy Hooks

---

## ✅ Заполнение базы данных (Seed)

- [ ] Запущен seed скрипт
- [ ] Создан контент для страницы "home"
- [ ] Создан администратор (email: admin@reems.com, password: Admin123!)

**Команда:**
```bash
railway run npm run prisma:seed
```

---

## ✅ Проверка работы приложения

- [ ] Открыта публичная страница: `https://your-app.railway.app/`
- [ ] Страница загружается корректно
- [ ] Контент отображается (блоки из базы данных)

- [ ] Открыта страница входа: `https://your-app.railway.app/admin/login`
- [ ] Страница входа загружается
- [ ] Успешный вход с учетными данными:
  - Email: `admin@reems.com`
  - Password: `Admin123!`

- [ ] Открыта админ-панель: `https://your-app.railway.app/admin/page-list`
- [ ] Отображается список страниц
- [ ] Можно открыть редактор страницы

- [ ] Открыт редактор: `https://your-app.railway.app/admin/pages/home`
- [ ] Отображаются все блоки
- [ ] Можно редактировать блок (изменить title, subtitle, body, bullets)
- [ ] Сохранение работает корректно
- [ ] Изменения отображаются на публичной странице

---

## ✅ Безопасность

- [ ] Пароль администратора изменен (после первого входа)
- [ ] `JWT_SECRET` достаточно сложный (минимум 32 символа, случайный)
- [ ] `.env` файл не попал в Git репозиторий

**Как изменить пароль админа:**
1. Войдите в админ-панель
2. Через Prisma Studio: `railway run npx prisma studio`
3. Или создайте скрипт для смены пароля

---

## 📋 Быстрые команды Railway CLI

Если используете Railway CLI:

```bash
# Установка CLI
npm i -g @railway/cli

# Вход в Railway
railway login

# Подключение к проекту
railway link

# Просмотр переменных
railway variables

# Запуск команды в окружении Railway
railway run <команда>

# Примеры:
railway run npx prisma migrate deploy
railway run npm run prisma:seed
railway run npx prisma studio
```

---

## 🆘 Если что-то пошло не так

### Приложение не запускается
- Проверьте логи: Railway Dashboard → Deployments → Последний деплой → View Logs
- Убедитесь, что все переменные окружения добавлены

### Ошибка "JWT_SECRET is not set"
- Проверьте, что `JWT_SECRET` добавлен в Variables основного сервиса
- Убедитесь, что название переменной точно `JWT_SECRET`

### Ошибка подключения к базе данных
- Проверьте, что PostgreSQL сервис запущен
- Убедитесь, что `DATABASE_URL` доступен в Variables основного сервиса
- Проверьте, что миграции применены

### Миграции не применяются
- Убедитесь, что используете команду: `npx prisma migrate deploy` (не `migrate dev`)
- Проверьте логи выполнения миграций

---

## 📚 Дополнительная документация

- [RAILWAY_DEPLOYMENT.md](./RAILWAY_DEPLOYMENT.md) - Подробная инструкция по деплою
- [RAILWAY_ENV_VARIABLES.md](./RAILWAY_ENV_VARIABLES.md) - Детальный список переменных
- [README.md](./README.md) - Общая документация проекта

---

**После прохождения всех пунктов чек-листа ваш проект готов к работе на Railway! 🚀**

